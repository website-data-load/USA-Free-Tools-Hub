(()=>{
  const REMOVED=new Set(['currency-converter','remove-background','pdf-to-word','percentage-calculator']);
  const activeTools=()=>TOOLS.filter(t=>!REMOVED.has(t.id));
  const escNav=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
  const cardNav=t=>`<a class="tool-card" href="#tool/${t.id}"><span class="tool-icon">${t.icon}</span><h3>${escNav(t.name)}</h3><p>${escNav(t.desc)}</p><span class="tag">100% FREE</span></a>`;
  function renderHome(){const list=activeTools();app.innerHTML=`<section class="hero"><span class="eyebrow">✦ ${list.length} free tools · No signup</span><h1>Free online tools for <span class="gradient-text">everyday tasks.</span></h1><p>Calculators, PDF tools, converters and text utilities. Every tool is free to use.</p><div class="search"><input id="q" placeholder="Search a tool…" aria-label="Search tools"><button class="btn" id="go">Search</button></div><div class="free-pill">✓ 100% free · No ads right now · No account required</div></section><section class="section" id="popular"><div class="section-head"><div><h2>Popular tools</h2><p>Quick access to everyday favorites.</p></div><a class="btn secondary" href="#tools">All tools</a></div><div class="tool-grid">${list.slice(0,8).map(cardNav).join('')}</div></section><section class="section" id="categories"><div class="section-head"><div><h2>Categories</h2><p>Find tools by purpose.</p></div></div><div class="category-grid">${[...new Set(list.map(t=>t.cat))].map(c=>`<a class="category" href="#category/${encodeURIComponent(c)}"><strong>${escNav(c)}</strong><span>${list.filter(t=>t.cat===c).length} free tools</span></a>`).join('')}</div></section>`;$('#go').onclick=()=>location.hash='search/'+encodeURIComponent($('#q').value)}
  function renderTools(q=''){const s=String(q).toLowerCase(),list=activeTools().filter(t=>!s||`${t.name} ${t.cat} ${t.desc}`.toLowerCase().includes(s));app.innerHTML=`<section class="section"><div class="section-head"><div><h1>All Free Tools</h1><p>${list.length} tools available.</p></div></div><div class="search" style="max-width:760px;margin:0 0 20px"><input id="q" value="${escNav(q)}" placeholder="Search tools…"><button class="btn" id="go">Search</button></div><div class="tool-grid">${list.length?list.map(cardNav).join(''):'<div class="tool-card"><h3>No tools found</h3><p>Try another search.</p></div>'}</div></section>`;$('#go').onclick=()=>location.hash='search/'+encodeURIComponent($('#q').value)}
  function renderWordCounter(t){page(t,`<div class="field"><label>Text</label><textarea id="txt" placeholder="Start typing…"></textarea></div><div class="stats"><div class="stat"><b id="w">0</b>Words</div><div class="stat"><b id="c">0</b>Characters</div></div><div class="result" id="read">Reading time: 0 min</div>`);$('#txt').oninput=()=>{const raw=$('#txt').value.trim(),words=raw?raw.split(/\s+/).length:0;$('#w').textContent=words;$('#c').textContent=$('#txt').value.length;$('#read').textContent=`Reading time: ${words?Math.max(1,Math.ceil(words/200)):0} min`}}
  async function renderWordToPdf(t){page(t,`<div class="tool-tabs" role="tablist"><button type="button" class="tab active" data-mode="type" role="tab" aria-selected="true">Type / Paste</button><button type="button" class="tab" data-mode="upload" role="tab" aria-selected="false">Upload Word Documents</button></div><div id="typed"><div class="field"><label>Document text</label><textarea id="text" placeholder="Type or paste your document text…"></textarea></div></div><div id="uploaded" hidden><div class="dropzone" id="drop"><strong>Select a Word document</strong><p>Upload a .docx file to convert its readable text to PDF.</p><input id="f" class="hidden" type="file" accept=".docx,application/vnd.openxmlformats-officedocument.wordprocessingml.document"></div></div><div class="actions"><button class="btn" id="run">Create PDF</button></div><div class="result" id="out">Choose Type / Paste or Upload Word Documents.</div>`);document.querySelectorAll('.tab').forEach(tab=>tab.addEventListener('click',()=>{document.querySelectorAll('.tab').forEach(x=>{x.classList.remove('active');x.setAttribute('aria-selected','false')});tab.classList.add('active');tab.setAttribute('aria-selected','true');const upload=tab.dataset.mode==='upload';$('#typed').hidden=upload;$('#uploaded').hidden=!upload;$('#out').textContent=upload?'Select a Word document to continue.':'Type or paste your document text to continue.'}));$('#drop')?.addEventListener('click',e=>{if(e.target.id!=='f')$('#f').click()});$('#f')?.addEventListener('change',()=>{const f=$('#f').files?.[0];if(f)$('#out').textContent=`Selected: ${f.name}`});$('#run').onclick=async()=>{try{let text=$('#text')?.value||'',file=$('#f')?.files?.[0];if(file){if(!file.name.toLowerCase().endsWith('.docx')){toast('Please choose a .docx Word document');return}if(!window.JSZip)await lib('https://cdn.jsdelivr.net/npm/jszip@3.10.1/dist/jszip.min.js');const zip=await JSZip.loadAsync(await file.arrayBuffer()),xmlFile=zip.file('word/document.xml');if(!xmlFile){toast('Invalid Word document');return}const xml=await xmlFile.async('string'),doc=new DOMParser().parseFromString(xml,'application/xml');text=[...doc.getElementsByTagName('w:t')].map(n=>n.textContent).join(' ').replace(/\s+/g,' ').trim();if(!text){toast('No readable text found in the Word document');return}}if(!text.trim()){toast('Type/paste text or upload a Word document');return}const JsPDF=await jsPDF(),pdf=new JsPDF(),lines=pdf.splitTextToSize(text,170);let y=20;for(const line of lines){if(y>280){pdf.addPage();y=20}pdf.text(line,20,y);y+=7}pdf.save('document.pdf');$('#out').textContent=file?'Word document converted to PDF.':'PDF created successfully.'}catch(err){console.error(err);$('#out').textContent='Could not convert this document. Please check the file and try again.'}}}
  const legal={privacy:{title:'Privacy Policy',intro:'Last updated: August 30, 2026',body:`<h2>Our approach</h2><p>USA Free Tools Hub is designed to minimize data collection. Core tools do not require an account.</p><h2>Browser processing</h2><p>Where a tool is described as browser-based, processing occurs on your device. Some optional features may load third-party libraries or services.</p><h2>Future advertising</h2><p>No advertising is displayed at launch. Advertising may be added in the future and this policy will be updated when applicable.</p><h2>Contact</h2><p>Use the <a href="#contact">Contact page</a> for privacy questions.</p>`},terms:{title:'Terms of Use',intro:'Last updated: August 30, 2026',body:`<h2>Use of the site</h2><p>You may use the tools for lawful personal or business purposes. Do not misuse the site, attempt unauthorized access, or interfere with its operation.</p><h2>Tool results</h2><p>You are responsible for checking important results before relying on them.</p><h2>Financial tools</h2><p>Mortgage, tax and other financial calculations are estimates and are not professional financial advice.</p><h2>File processing</h2><p>Do not upload content you do not have the right to process.</p>`},disclaimer:{title:'Disclaimer',intro:'Important information about tool results and estimates.',body:`<p>USA Free Tools Hub provides general-purpose tools and estimates. Results may depend on browser support, source files, external data and changing rules.</p><p>Tax, mortgage and other financial results are estimates for informational purposes only and are not tax, legal, investment or financial advice.</p><p>For important decisions, verify results with authoritative sources or a qualified professional.</p>`},contact:{title:'Contact',intro:'We welcome feedback about broken tools, accessibility and usability.',body:`<h2>Get in touch</h2><p>Add your preferred public contact email here before launch. We do not invent or publish an email address on your behalf.</p>`}};
  function showLegal(key){const x=legal[key];if(!x)return;app.innerHTML=`<section class="tool-page"><div class="crumb"><a href="#home">Home</a> / ${escNav(x.title)}</div><h1>${escNav(x.title)}</h1><p class="tool-intro">${x.intro}</p><div class="tool-content">${x.body}</div></section>`;window.scrollTo({top:0,behavior:'smooth'})}
  function route(){const raw=location.hash.replace(/^#/,'')||'home',parts=raw.split('/'),value=decodeURIComponent(parts.slice(1).join('/'));if(raw==='home'){renderHome();return}if(raw==='tools'){renderTools();return}if(raw==='about'){about();return}if(legal[raw]){showLegal(raw);return}if(parts[0]==='search'){renderTools(value);return}if(parts[0]==='category'){renderTools(value);return}if(parts[0]==='tool'){if(REMOVED.has(value)){location.hash='#tools';return}const t=activeTools().find(x=>x.id===value);if(!t){renderTools();return}if(value==='word-counter'){renderWordCounter(t);return}if(value==='word-to-pdf'){renderWordToPdf(t);return}render(t);return}renderHome()}
  document.addEventListener('click',e=>{const a=e.target.closest('a[href="privacy.html"],a[href="terms.html"],a[href="disclaimer.html"],a[href="contact.html"]');if(!a)return;e.preventDefault();location.hash='#'+a.getAttribute('href').replace('.html','')});document.addEventListener('click',e=>{const a=e.target.closest('a[href]');if(!a)return;const m=a.getAttribute('href')?.match(/^#tool\/(.+)$/);if(m&&REMOVED.has(m[1])){e.preventDefault();location.hash='#tools'}});window.addEventListener('hashchange',route);route();
})();